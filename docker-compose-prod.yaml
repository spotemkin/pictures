version: '3.9'

services:
  pic-main:
    build: .
    container_name: pic-main
    restart: always
    volumes:
      - /data/pics/auto:/data/pics/auto
    networks:
      - traefik

networks:
  traefik:
    external: true
    name: traefik